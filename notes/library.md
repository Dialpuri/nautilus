The library is iterated through in the add_pdb function and is stored in the nadb std::vector.
The last monomer in this library is used as an representative na (narepr) which is used in the find function.
Find searches for a link in the monomer library based on the distance between O3 and P.
